# example-extension SPEC

## Purpose

One-paragraph statement of why this extension exists and what user pain it solves. Replace this section first.

## Behaviors

### Commands

- `/example-extension` — what it does, when the user runs it, what it produces.

### Events

- `session_start` — what happens when a Pi session begins.
- `session_shutdown` — what cleanup runs when a session ends.

## Non-goals

- Things this extension explicitly does _not_ try to do, so future-you doesn't accidentally scope-creep into them.
