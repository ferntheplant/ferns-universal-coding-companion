{
  "name": "ferns-universal-coding-companion",
  "private": true,
  "workspaces": {
    "packages": [
      "extensions/*",
      "plugins/*",
      "skills/*",
      "templates/*"
    ],
    "catalog": {
      "@earendil-works/pi-coding-agent": "^0.74.0",
      "@earendil-works/pi-ai": "^0.74.0",
      "@earendil-works/pi-tui": "^0.74.0",
      "@types/bun": "latest",
      "typescript": "^5.9.3"
    }
  },
  "type": "module",
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test",
    "new-extension": "bun run scripts/new-extension.ts",
    "lint": "oxlint",
    "format": "oxfmt --write",
    "format:check": "oxfmt --check",
    "check": "bun run typecheck && bun run lint && bun run format:check"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "oxfmt": "0.46.0",
    "oxlint": "1.61.0",
    "typescript": "catalog:"
  }
}
